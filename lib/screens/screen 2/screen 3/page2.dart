import 'package:flutter/material.dart';

class BoxDec extends StatelessWidget {
  const BoxDec({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: SizedBox(
        width: 354,
        height: 496,
      ),
    );
  }
}
